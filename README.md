This is an attempt to have a jenkins pipeline to deploy aws infra via terraform.

To begin with, it has terraform init, plan, apply and show as separate piplines.

Special thanks to alexandarp on github.
